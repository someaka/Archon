# HANDOFF — 2026-04-30 Verifier Fix Round

> Status: **Committed & Pushed** to `origin/dev` as `f0a714a7`
> Branch: `dev` | Repo: `/home/d/Desktop/Archon-canonical`

---

## What Was Done

### Verifier Results (17 bugs found)

- **Hermes** reviewed Pi code → 7 bugs
- **Pi** reviewed Hermes code → 10 bugs
- **Claude** smoke test → passed (Ollama gateway, deepseek-v4-pro:cloud)

### Fixes Applied (committed in f0a714a7)

**Hermes provider (10 fixes):**

- `event-bridge.ts`: stdin error listener leak, drain listener leak, unhandled executePrompt rejection, activeTimers leak
- `provider.ts`: lock held across retries (14s starvation) → release during backoff
- `session-pool.ts`: acquire() hands out dead sessions, release() resurrects zombies, get() resets idle timer
- `acp-client.ts`: spawn error capture, mutual exclusion guard, unref(), deferred kill

**Pi provider (5 fixes):**

- `provider.ts`: process.env cleanup in finally, mkdtemp for tmpdir, step renumbering
- `event-bridge.ts`: manual reverse scan
- `ui-context-stub.ts`: throw on unsupported custom() dialog

**Tests:** 19 new regression tests across 5 test files

### Lint Fixes (6 errors caught by pre-commit hook)

- Added return type annotations to 4 arrow functions
- Added eslint-disable for intentional `delete process.env[key]` and empty catch

---

## What's Still Open

### 2 Failing Tests (pre-existing / known)

### 2 Failing Tests (pre-existing / known)

1. **`ClaudeProvider > passes correct options to SDK`** — pre-existing, not caused by our changes. Fails on all runs.
2. ~~`assistantConfig.env applies to process.env when not already set`~~ — **FIXED** in f0152013 (assertion updated to match cleanup behavior)

### 2 Bugs Not Fixed

1. **AsyncQueue `push(undefined)` guard** — `yield undefined` from async generator causes JS runtime to treat as `done: true`. The `buffer.length > 0` fix doesn't help. Needs sentinel wrapper approach (e.g., `{ value: T, done: false }` envelope).
2. **Static Pi SDK imports in options-translator.ts** — can't convert to dynamic imports without modifying test files (tests don't set up PI_PACKAGE_DIR shim). Left as-is since the file is only loaded via dynamic import from provider.ts.

### Type Errors (pre-existing)

- `@mariozechner/pi-coding-agent` has type mismatches: missing `codingTools` export, `agentDir` property, `setWorkingVisible`/`setWorkingIndicator`/`addAutocompleteProvider` on ExtensionUIContext
- These existed before our changes and are NOT regressions

### Uncommitted Plan Files

These are in the commit but are internal planning docs:

- `.hermes/plans/2026-04-30-PI-FIXES.md`
- `.hermes/plans/2026-04-30-HERMES-FIXES.md`
- `.hermes/plans/2026-04-30-ARCHITECTURE-FIXES.md`
- `.hermes/plans/2026-04-30-VERIFIER-HANDOFF.md`
- `.hermes/verifier-results.md`

---

## User's Setup (CRITICAL — read this first)

- **NO Anthropic products** — never assume API keys or Claude auth
- **Claude Code**: `ollama launch claude --model deepseek-v4-pro:cloud` (custom Ollama v0.20.6 gateway)
- **Pi**: `opencode-go/kimi-k2.6` via OPENCODE_API_KEY
- **Hermes**: `mimo-v2.5-pro` via xiaomi endpoint
- Ollama gateway env vars: ANTHROPIC_BASE_URL=http://127.0.0.1:11434, ANTHROPIC_API_KEY=ollama

## Key Lessons

- Never stash before verification — commit first, then validate
- Pre-commit hook catches lint errors — always run `bun run validate` before pushing
- `process.wait` is clamped to 180s — use `notify_on_complete` for long workflows
- Pi with kimi-k2.6 can take 24+ minutes due to extended thinking
